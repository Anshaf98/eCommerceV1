import React from "react";
import ProductItems from "../components/ProductSection/ProductItems";

const Products = () => {
  return (
    <div>
      <ProductItems />
    </div>
  );
};

export default Products;
